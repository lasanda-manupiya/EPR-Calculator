import React, { useState, createContext, useContext } from 'react';
import ReactDOM from 'react-dom/client';
import { BrowserRouter, Navigate, Route, Routes, Link, useNavigate } from 'react-router-dom';
import axios from 'axios';
import './index.css';
const api = axios.create({ baseURL: 'http://localhost:4000/api' });
const Auth = createContext<any>(null); const useAuth=()=>useContext(Auth);
const Card=({children}:{children:any})=><div className='bg-white p-4 rounded-xl shadow'>{children}</div>;
const Layout=({children}:{children:any})=>{const {logout}=useAuth(); return <div className='min-h-screen flex'><aside className='w-64 bg-brand text-white p-4 space-y-2'><h1 className='text-xl font-bold'>SustainZone</h1>{['/dashboard','/products','/library','/reports','/settings'].map((p)=><Link key={p} className='block hover:bg-white/20 p-2 rounded' to={p}>{p.slice(1).replace(/^./,c=>c.toUpperCase())}</Link>)}<button onClick={logout}>Log out</button></aside><main className='flex-1 p-6'>{children}</main></div>};
const Login=()=>{const {setToken}=useAuth();const nav=useNavigate();const [email,setEmail]=useState('demo@sustainzone.co.uk');const [password,setPassword]=useState('DemoPass123!');return <div className='max-w-md mx-auto mt-24'><Card><h2>Log in</h2><input className='border p-2 w-full my-2' value={email} onChange={e=>setEmail(e.target.value)}/><input type='password' className='border p-2 w-full my-2' value={password} onChange={e=>setPassword(e.target.value)}/><button className='bg-brand text-white px-4 py-2' onClick={async()=>{const r=await api.post('/auth/login',{email,password});setToken(r.data.token);nav('/dashboard')}}>Sign in</button><Link to='/register' className='ml-2 text-brand'>Register</Link></Card></div>};
const Register=()=>{const nav=useNavigate(); const [f,setF]=useState({name:'',email:'',password:'',company_name:''}); return <div className='max-w-md mx-auto mt-24'><Card><h2>Register</h2>{Object.keys(f).map(k=><input key={k} placeholder={k} className='border p-2 w-full my-2' value={(f as any)[k]} onChange={e=>setF({...f,[k]:e.target.value})}/>)}<button className='bg-green text-white px-4 py-2' onClick={async()=>{await api.post('/auth/register',f);nav('/')}}>Create account</button></Card></div>};
const Dashboard=()=>{const [s,setS]=useState<any>(); const {token}=useAuth(); React.useEffect(()=>{api.get('/reports/summary',{headers:{Authorization:`Bearer ${token}`}}).then(r=>setS(r.data));},[]); if(!s)return null; return <Layout><div className='grid md:grid-cols-3 gap-4'><Card><p>Products assessed</p><p className='text-3xl'>{s.numberOfProducts}</p></Card><Card><p>Total packaging</p><p className='text-3xl'>{s.totalPackagingWeight.toFixed(2)} g</p></Card><Card><p>Method</p><p>{s.estimationMethod}</p></Card></div></Layout>};
const Products=()=>{const {token}=useAuth(); const [items,setItems]=useState<any[]>([]); const [p,setP]=useState<any>({product_name:'',category:'',sku:'',length:0,width:0,height:0,unit:'mm',quantity:1,packaging_components:[{material_type:'Cardboard',packaging_type:'primary',length:100,width:100,height:100,unit:'mm'}]}); const load=()=>api.get('/products',{headers:{Authorization:`Bearer ${token}`}}).then(r=>setItems(r.data)); React.useEffect(load,[]); return <Layout><Card><h2>Create product wizard (MVP)</h2><div className='grid md:grid-cols-3 gap-2'>{['product_name','category','sku','length','width','height','quantity'].map(k=><input key={k} placeholder={k} className='border p-2' onChange={e=>setP({...p,[k]:e.target.value})}/>)}<select className='border p-2' onChange={e=>setP({...p,packaging_components:[{...p.packaging_components[0],material_type:e.target.value}]})}>{['Cardboard','Plastic','Paper','Glass','Aluminium','Steel','Wood','Other'].map(m=><option key={m}>{m}</option>)}</select><select className='border p-2' onChange={e=>setP({...p,packaging_components:[{...p.packaging_components[0],packaging_type:e.target.value}]})}>{['primary','secondary','tertiary'].map(m=><option key={m}>{m}</option>)}</select></div><button className='mt-3 bg-brand text-white px-4 py-2' onClick={async()=>{await api.post('/products',p,{headers:{Authorization:`Bearer ${token}`}});load();}}>Save product</button></Card><Card><h3>Products</h3><table className='w-full'><tbody>{items.map(i=><tr key={i.id}><td>{i.product_name}</td><td>{i.category}</td><td>{i.quantity}</td></tr>)}</tbody></table></Card></Layout>};
const Library=()=>{const {token}=useAuth(); const [rows,setRows]=useState<any[]>([]); React.useEffect(()=>{api.get('/library',{headers:{Authorization:`Bearer ${token}`}}).then(r=>setRows(r.data));},[]); return <Layout><Card><h2>Reference library</h2>{rows.map(r=><div key={r.id} className='border-b py-1'>{r.reference_name} - {r.material_type} - {r.average_weight}g</div>)}</Card></Layout>};
const Reports=()=>{const {token}=useAuth(); return <Layout><Card><h2>Reports</h2><div className='space-x-3'><a className='text-brand' href={`http://localhost:4000/api/reports/export.csv`} onClick={e=>{e.preventDefault();window.open(`http://localhost:4000/api/reports/export.csv?token=${token}`)}}>Export CSV</a><a className='text-brand' href='http://localhost:4000/api/reports/export.pdf' target='_blank'>Export PDF</a></div></Card></Layout>};
const Settings=()=> <Layout><Card>Settings</Card></Layout>;
const Protected=({children}:{children:any})=>{const {token}=useAuth(); return token?children:<Navigate to='/'/>};
const App=()=>{const [token,setToken]=useState(localStorage.getItem('token')); React.useEffect(()=>{token?localStorage.setItem('token',token):localStorage.removeItem('token');},[token]); api.interceptors.request.use(c=>{if(token)c.headers.Authorization=`Bearer ${token}`; return c;}); return <Auth.Provider value={{token,setToken,logout:()=>setToken(null)}}><BrowserRouter><Routes><Route path='/' element={<Login/>}/><Route path='/register' element={<Register/>}/><Route path='/dashboard' element={<Protected><Dashboard/></Protected>}/><Route path='/products' element={<Protected><Products/></Protected>}/><Route path='/library' element={<Protected><Library/></Protected>}/><Route path='/reports' element={<Protected><Reports/></Protected>}/><Route path='/settings' element={<Protected><Settings/></Protected>}/></Routes></BrowserRouter></Auth.Provider>};
ReactDOM.createRoot(document.getElementById('root')!).render(<App />);
